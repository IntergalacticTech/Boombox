# /etc/asound.conf — default ALSA device for the boombox.
# `type plug` lets clients ask for any rate/format and have ALSA
# resample/transform on the way to the HiFiBerry DAC.

pcm.!default {
    type plug
    slave.pcm "hw:sndrpihifiberry"
}

ctl.!default {
    type hw
    card sndrpihifiberry
}
