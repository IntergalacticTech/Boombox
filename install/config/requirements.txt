# Python deps for the boombox-* services.
# Installed into /opt/boombox/.venv by install.sh.

aiohttp>=3.9
websockets>=12.0
numpy>=1.26

# boombox-buttons
gpiod>=2.1

# services/boombox-updater — PEP 440 version compare
packaging>=24.0

# boombox-bt-volume — uses system dbus-python + PyGObject (apt-installed,
# not pip; they need GLib/D-Bus headers that won't build cleanly in a venv).
# The venv is created with --system-site-packages so we can import them.

# services/boombox-library — YAML config + at-rest password encryption
PyYAML>=6.0
cryptography>=42

# services/boombox-remote — mDNS advertisement of the remote API
zeroconf>=0.130

# services/ble_peripheral — BLE transport for the wireless remote.
# boombox-remote degrades gracefully without it, but install it so BLE
# pairing works out of the box. (Was hand-installed on boombox1 and
# missing from here — drift caught bringing up boombox2.)
bless>=0.2

# services/boombox-library art proxy — cover-art transcode/resize
Pillow>=10.0

# Dev/test (installed in the venv even on the Pi so /opt/boombox can self-test).
pytest>=8.0
pytest-asyncio>=0.23
pytest-aiohttp>=1.0
watchdog>=4.0
