# Python deps for the boombox-* services.
# Installed into /opt/boombox/.venv by install.sh.

aiohttp>=3.9
websockets>=12.0
numpy>=1.26

# boombox-buttons
gpiod>=2.1

# boombox-bt-volume — uses system dbus-python + PyGObject (apt-installed,
# not pip; they need GLib/D-Bus headers that won't build cleanly in a venv).
# The venv is created with --system-site-packages so we can import them.

# Dev/test (installed in the venv even on the Pi so /opt/boombox can self-test).
pytest>=8.0
watchdog>=4.0
